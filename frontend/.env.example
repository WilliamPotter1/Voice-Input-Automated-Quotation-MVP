# Backend API URL (optional).
# - Leave empty in development: requests go to /api (Vite proxy to localhost:3001).
# - Set for production: your deployed backend URL (no trailing slash).
# Example:
# VITE_API_URL=https://voice-input-automated-quotation-mvp-vert.vercel.app

VITE_API_URL=
